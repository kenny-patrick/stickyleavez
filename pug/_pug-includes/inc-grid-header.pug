header.product-grid-header
    .mr-3.mb-3 
        | Showing 
        strong 1-12 
        | of 
        strong 158 
        | products
    
    .mr-3.mb-3
        span.mr-2 Show
        - var show = []
        - show.push({count: 12, activeClass: 'active'})
        - show.push({count: 24, activeClass: ''})
        - show.push({count: 'All', activeClass: ''})

        each val in show
            a(href="#", class="product-grid-header-show " + val.activeClass) #{val.count}    
        

    .mb-3.d-flex.align-items-center
        span.d-inline-block.mr-1 Sort by
        select.custom-select.w-auto.border-0
            - var orderBy = ['Default', 'Popularity', 'Rating', 'Newest first']
        
            each val, index in orderBy
                option(value='orderby_' + index) #{val}